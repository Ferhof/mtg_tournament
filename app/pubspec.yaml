name: mtg_tournament_app
description: Offline MTG tournament manager (Swiss + top cut).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.0
  uuid: ^4.0.0
  mtg_tournament_engine:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
